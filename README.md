# jiralert-config
Jiralert and Alertmanager configuration files

Since Jiralert documentation is rather lacking and spread through github issues, here is a working configuration example, tested in production.
